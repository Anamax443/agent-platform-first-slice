{
  "$comment": "Per-deployable overlay merged onto deploy/cloudflare/<deployable>/wrangler.jsonc by scripts/farm-config.mjs. Everything here is installation-specific (routes, addresses); the base configs stay free of it. The generator itself adds vars.INSTALLATION and the apf:installation alias, so only what differs from the base belongs here.",
  "apf-gateway": {
    "routes": [{ "pattern": "apf.maxferit.cz", "custom_domain": true }]
  },
  "apf-email-executor": {
    "vars": { "EMAIL_FROM": "apf-notify@maxferit.cz", "EMAIL_FROM_NAME": "agent-platform-farm" }
  },
  "apf-mail-ingest": { "vars": { "INTAKE_ADDRESS": "apf-intake@maxferit.cz" } }
}
