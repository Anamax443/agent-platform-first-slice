{
  "$comment": "Platform policy (ADR-016): authority artefact. Grants scopes per tenant AND the recipient allowlist: the executor resolves recipientRef through this table, so no payload can name an address. The AI identity is deliberately absent (F1). Times UTC with Z.",
  "policyRef": "policy/email.send/v1",
  "capability": "email.send",
  "capabilityVersion": "1",
  "owner": "platform",
  "validFrom": "2026-09-06T00:00:00Z",
  "grants": [
    { "actorId": "svc-orchestrator", "actorType": "service", "scopes": ["email.send"], "tenants": ["tenant-42"], "rateLimit": { "commandsPerMinute": 60 } },
    { "actorId": "svc-orchestrator-t7", "actorType": "service", "scopes": ["email.send"], "tenants": ["tenant-7"], "rateLimit": { "commandsPerMinute": 60 } }
  ],
  "approval": { "required": false },
  "recipientAllowlist": {
    "tenant-42": { "ops-mailbox": "ops@maxferit.example", "supervisor-mailbox": "supervisor@maxferit.example" },
    "tenant-7": { "ops-mailbox": "ops@tenant7.example" }
  },
  "isolation": { "acceptedIsolationClass": "PRINCIPAL", "isolationDecisionRef": "riskClass MEDIUM; own execution context email-executor with its own credential domain (cred:smtp only); in this slice simulated as a separate host + resolver in one process, real boundary = separate deployable (PLATFORM-NOTES §7)" },
  "failClosed": true
}
