# MEASUREMENT — co norma stála a co zachytila

Jediná metrika, která rozhodne o hodnotě normy (`agent-platform-foundation`, část XVI.4):

> Kolik práce navíc norma vytvořila, a kolikrát test zachránil chybu dřív, než by ji našel provoz.

K tomu lean pohled (dotaz vlastníka 6. 9. 2026): rozlišovat čas přidávající hodnotu od plýtvání, a plýtvání pojmenovat kategorií, ne pocitem.

## Pravidla měření

- Zapisuje se po každém milníku XII.G (M1 descriptor a kontrakty, M2 conformance balíček, M3 testy podle profilů, M4 druhý tok, M5 evidence, M6 balíček kontraktů).
- **Čas je AI-asistovaný wall-clock**, ne hodiny solo operátora. Limit 40 hodin na MUST sadu (VC §7) je limit na lidskou práci; proto se zvlášť zapisuje **čas vlastníka** (čtení, review, rozhodnutí). Bez toho by měření lhalo.
- Každý nález zachycený testem se zapíše s Test ID a s tím, kde by se jinak projevil (provoz, review, nikdy).
- WIP limit: **jeden řez najednou.** Druhý tok začne až po uzavření M3 prvního.
- **Čas vlastníka se člení** (posudky 1 a 2): architektura a rozhodování (čtení normy, volba komponent, návrh descriptorů) · review výstupů · implementace handlerů · testy a fixtures · ladění nálezů (12 nálezů × čas opravy) · dokumentace · provoz (login, DNS, secrets, účty). Součet je skutečná cena; AI wall-clock je jen jedna položka vedle něj. Hodnoty doplní vlastník, asistent je nemůže odhadnout. Překročí-li součet za M1–M4 100 h, přehodnotit III §7 podle XII.G.
- **Mezní cena domény** (posudek 1, doporučení 4): u každého dalšího toku se zapíše „platforma +řádků" zvlášť od komponenty, fixtures a testů. M4 = 45. Poroste-li platforma s každou doménou o stovky řádků, LEGO hypotéza slábne a je to nález, ne šum.

## Lean kategorie plýtvání (muda), jak je tu chápeme

| Kategorie | Co to tady znamená |
|---|---|
| nadvýroba | kód nebo test, který žádný scénář ani Test ID nevyžaduje |
| zásoba | dokumentace a fixtures, které nikdo nečte ani nespouští |
| čekání | blokováno na rozhodnutí vlastníka nebo na externí systém |
| nadměrné zpracování | boilerplate, který norma vyžaduje bez doložené hodnoty (kandidát na generátor) |
| vady | chyba nalezená později, než mohla být (test existoval, ale neběžel; test chyběl) |
| pohyb | přepínání kontextu mezi repozitáři a dokumenty |
| přeprava | ruční kopírování dat mezi kontrakty, descriptory a testy (kandidát na generování) |

## Záznamy

Wall-clock je odečten z časových značek commitů a ze začátku/konce session; není to stopky.

**Čas vlastníka (odhad vlastníka 6. 9. 2026, na čtvrthodiny, za M0–M4b celkem, potvrzeno „sedí"):**

| Kategorie | Hodin | Co v ní bylo |
|---|---|---|
| architektura a rozhodování | 1,5 | čtení návrhových listů a zadání, rozhodnutí „ano / up to you", pořadí kroků |
| posudky a review | 2 | čtení STATUS, sehnání a postoupení čtyř posudků, protokol |
| ladění a zásahy | 0,25 | vlastní opravy a vracení |
| provoz a účty | 0,5 | `wrangler whoami`, Cloudflare, GitHub |
| **celkem** | **4,25** | |

Odhad je zpětný, ne stopky, a nejde rozdělit po milnících; v řádcích tabulky proto zůstává „nezměřeno" a celek platí pro všechny řádky dohromady. Od tohoto odhadu se měří dál po řádcích: krok 3 „lite" (6. 9. odpoledne) + 0,25 h provoz (Access aplikace, service token, politika) → **4,5 h celkem**.

| Milník | Datum | Wall-clock AI | Čas vlastníka | Řádků (+) | Z toho kontrakt / descriptor / testy / boilerplate / business | Nálezy zachycené testem | Plýtvání (kategorie, minuty) |
|---|---|---|---|---|---|---|---|
| M0 kostra repa | 2026-09-05 | nezměřeno (session před 21:10) | nezměřeno | ~700 | kontrakty zkopírované z foundation (5 schémat + vzor policy), zbytek dokumentace | 0 | pohyb: čtení foundation |
| M1 descriptory + platformové minimum | 2026-09-05 | ~17 min do přerušení (commit 21:27) + ~15 min dokončení handlerů v další session | nezměřeno | platform 1 836, adaptéry 172, komponenty 327 ts + 310 json, slice 170, policy 92, workflow 53 | kontrakt 455 (schémata in/out, policy, workflow) / descriptor ~130 / testy 0 / boilerplate 2 178 (platform + adaptéry + slice) / business 327 | 1 (smoke test: oddělovače v promptu, viz N3) | přeprava 5 (enum typů dokumentu v 6 schématech); vady 5 (prompt) |
| M2 conformance balíčky | 2026-09-05 | ~10 min | nezměřeno | conformance 795 (602 json, 193 md) + harness 331 | testy 1 126 (fixtures, golden, errors.md, runner) | 1 (CTR-ERR-001: deklarovaný kód, který nikdo nevydává, viz N2) | nadvýroba 10 (document.archive handler + 4 fixtures jen kvůli realitě sdíleného hostu); vady 2 (řídicí znaky v JSON fixture, nástroj) |
| M3 testy podle profilů | 2026-09-05 | ~20 min (9 souborů, 132 testů, 44 Test ID) | nezměřeno | tests 1 111 + scripts 73 | testy 1 184 | 2 (EVD-004, IDM-DEADLINE-002, viz N1, N4) + 4 nálezy z psaní testů proti kódu (N5–N8) | pohyb 8 (čtení VC §4–§7 a XII.G); nadměrné zpracování 5 (errors.md ručně vs. errorCodes v descriptoru) |
| **M1–M3 celkem** | | **≈ 1 h 15 min AI wall-clock** | nezměřeno | ≈ 5 700 | kontrakt 455 / descriptor 130 / testy 2 310 / boilerplate 2 178 / business 327 | **9** | ≈ 35 min |
| M4 druhý tok (mail.ingest → document.* → email.send) + W4 druhý signál | 2026-09-05/06 | ≈ 35 min ve dvou úsecích (zápis večer, spuštění testů ráno) | nezměřeno | ≈ 970: komponenty 286 (ts + json), adaptér smtp 63, platforma +45 (kapacita, vnořené inputs, deadline per krok, workflow guard), slice +64, policy 34, workflow 75, conformance 430 (2 balíčky + golden master), testy 274 + harness 70 | kontrakt 109 / descriptor ~90 / testy 774 / boilerplate 172 / business ~150 | 1 (INT-E2E-001 + SEC-CTX-003 při prvním běhu, viz N12) + 1 z návrhu (N10) + 1 potvrzení W4 (N11) | vady 8 (dva pády prvního běhu: podvržený scope kolidoval s novým legitimním scope, fixture injekce bez těla); nadvýroba 0 (archive z M2 už slouží jako druhý handler) |
| M4b krok 1: portabilita platformy + instalační profil (bez cloudu) | 2026-09-06 | ≈ 1 h 10 min ve dvou úsecích (10:58–11:37 rozpracováno a přerušeno v červeném stavu; 11:37–12:15 zezelenat, lint, farm-config, docs) | nezměřeno | ≈ 1 010 (+) / 175 (−): src +295/−112 (installation 105, transport 61, workflow 30, slice, schemas, policy), config 218 (schéma profilu 75, 2 profily, 12 policy, farm.json), schéma workflow 51, testy +181, scripts +246 (lint instalačních hodnot, farm-config, jsonc), deploy ±7 | kontrakt 269 (profil, policy, schéma workflow) / descriptor 0 / testy 181 / boilerplate ≈ 540 (platforma, slice, skripty) / business 0 | 0 nálezů v kódu; lint při prvním běhu potvrdil 4 instalační hodnoty v base `wrangler.jsonc` (doména, adresa a jméno odesílatele, routes) z auditu 6. 9.; 8 testů INST dokládá fail-closed cesty profilu, tabulky credentialů a workflow definic | přeprava 10 (policy `git mv` z `contracts/` do dvou instalací); vady 5 (jeden červený commit mezi celky: pravidlo „malé celky = milníky" vzniklo až během kroku); nadvýroba 0 |
| M4b krok 2, celek 1: instalace do Workeru, validátor bez generování kódu, rozhodnutí o adresách | 2026-09-06 | ≈ 45 min aktivní práce mezi commity 12:06 a 16:19 (zbytek intervalu: plán kroku 2, rozhodnutí vlastníka „up to you", zápis času vlastníka) | nezměřeno | ≈ +250 / −60: schemas 55, bytes 35, ids/signing/artifacts ±15, farm-config +50, farm-check, d.ts 10, gateway +25, deploy tsconfig, config farm-bass443, inst test +15 | kontrakt 0 / descriptor 0 / testy 15 / boilerplate ≈ 200 / business 0 | 2 nálezy mimo testy (W13 Ajv generuje kód, ve Workeru zakázáno; W14 `Buffer` typová kolize workers-types × @types/node); oba by `wrangler deploy --dry-run` nechytil, důkaz = první `/version` z workerd pro obě instalace | vady 10 (dva pokusy s typy pro deploy, než se našel `Buffer: any` ve workers-types); nadvýroba 0 |
| M4b krok 3 „lite": skeleton nasazen na bass443 za Access, ověřen z internetu | 2026-09-06 | ≈ 30 min aktivní práce mezi commity 16:28 a 17:00 | ≈ 0,25 h (Access aplikace, service token, politika, kontrola v prohlížeči) | +60 (`farm-deploy.mjs`), `farm.json` s id prostředků, `.env.example` | boilerplate 60 | 0 nálezů v kódu; provozní zjištění: (a) gateway a hosty se navzájem odkazují service bindingy → první nasazení dvoufázově (bootstrap bez `services`); (b) znovupoužitelná Access politika musí být k aplikaci výslovně připojená, jinak service token dostane 302 („Used by applications: 0" to prozradí); `/version` 200 přes login i token, `/dispatch` 501 | vady 5 (jeden neúspěšný test tokenu kvůli nepřipojené politice); nadvýroba 0 |
| M4b krok 2, celek A: příjem dokumentu na farmě (stránka, DO journal + audit + artefakty, kopie R2 + D1, stránka instance) | 2026-09-06 | ≈ 1 h 50 min (17:00–19:05, z toho ≈ 15 min rozhovor o podobě dílčí kontroly) | ≈ 0,1 h (zadání „stránka, nahrát, dílčí výstup") | ≈ +260 / −47: platforma +30 (rozhraní `JournalStore`/`AuditTrail`, `workflowId` preset, registr workflow do `workflow.ts`), gateway +330 (store 150, page 110, index +70), lint −1 TLD | kontrakt 0 / descriptor 0 / testy 0 / boilerplate ≈ 250 / business 0 (stránka je evidence, ne app) | 0 nálezů testem; návrhové rozhodnutí W15 (synchronní úložiště platformy vs. asynchronní R2/D1: DO SQLite je zdroj pravdy, kopie ven po běhu); nástroj: relativní import v ambientním `declare module` je pod `skipLibCheck` tiše `any` (chyba TS2439 skrytá) → inline `import()` typ; lint falešně chytil `r.at` v šabloně (TLD `.at`, `.de` vyřazeny, label ≥ 2 znaky) | vady 10 (typ `any` z d.ts, falešný nález lintu); nadvýroba 0; přeprava 0 |
| M4b krok 2, celek A2: PDF / fotka / docx → text (binární originál v R2, Workers AI toMarkdown, derivace s provenancí) | 2026-09-06 | ≈ 25 min (19:05–19:30) | ≈ 0,05 h (připomínka „faktury jsou v PDF a jako příloha e-mailu") | ≈ +212 / −48: platforma +12 (`sha256Bytes`, metadata artefaktu), gateway store +40, page +30, index +90 | boilerplate ≈ 170 / business 0 | 0 nálezů; extrakce zatím není capability s descriptorem (W16); výstup `toMarkdown` začíná blokem `## Metadata` s vlastnostmi PDF, který uvidí i classify (data, ne instrukce, ale rozšiřuje vstup modelu) | vady 0; nadvýroba 0 |
| M4b krok 2, celek B: podepsaný dispatch, classify se skutečným modelem a výběrem modelů z profilu, validate; workflow v2 | 2026-09-06 | ≈ 1 h (19:35–20:40, z toho ≈ 10 min rozhovor o modelech a NIS2) | ≈ 0,1 h (pokyny: modely free/placené, NIS2/ISO, ISDOC) | ≈ +700 / −40: profil schéma +35, installation +45 (`ModelOption`, `modelTable`), adaptéry workers-ai 30 + anthropic 45, classifier +6, workflow v2 ×2 (+130, v1 nedotčeno), registr verzí +20, gateway wiring 120 + index +80 + page +30, testy +40 (INST-005, WF-VER-001 podle verze), lint adaptérů | kontrakt 165 (profil, v2 definice) / descriptor 0 / testy 40 / boilerplate ≈ 350 / business ≈ 80 (adaptéry) | 2 nálezy v provozu: (a) `@cf/meta/llama-3.1-8b-instruct` už na Workers AI neexistuje, `MODEL_UNAVAILABLE` bez důvodu → přidán `reason` a `modelId`; (b) skutečný model (Llama 3.1 8B) odolal jednoduché injekci, gullible fake ne, druhý signál validátoru chytil obojí (W4 živě); 1 nález testem: WF-VER-001 měl verzi „2" natvrdo, po vzniku v2 padl → verze „o jedna vyšší" | vady 10 (špatné id modelu, jeden běh navíc); nadvýroba 0; čekání 0 |
| **M1–M4 celkem** | | **≈ 1 h 50 min AI wall-clock** | 4,25 h za M0–M4b celkem (odhad vlastníka, viz výše) | ≈ 6 670 | kontrakt 564 / descriptor 220 / testy 3 084 / boilerplate 2 350 / business ~480 | **12** | ≈ 45 min |
| M4b krok 2, celek C: `document.validate` přes skutečnou síť (`apf-fakes` service binding), registr, DMS a archiv za jedním HTTP protokolem s chaos přepínači v KV | 2026-09-06 | ≈ 25 min (20:48–21:13, po HANDOFF (19) na začátku session) | nezměřeno | ≈ +480 / −27: nový `src/adapters/fakes-http.ts` 182 (protokol + chaos + tři doubly), `apf-fakes` Worker přepsán na tenký binding (37), `registry.ts` +43 (`HttpRegistryAdapter`, `RegistryUnavailable` s detailem), `platform-wiring.ts`/`index.ts`/`page.ts`/`slice.ts` ±30 (registr injektovaný, ne fake v procesu), testy `tests/fakes.test.ts` 185 (8 testů, Test ID řezu `INT-HTTP-001..008`) | kontrakt 0 / descriptor 0 / testy 185 / boilerplate ≈ 295 / business 0 | 0 nálezů v kódu; ověřeno `wrangler dev -c apf-gateway -c apf-fakes` (multi-worker dev, service binding [connected]) proti `local-fakes`: `/version` hlásí `apf-fakes` odpověď přes binding, dokument prošel `classify:SUCCEEDED` → `validate:SUCCEEDED` (registr přes síť) → `stamp:FAILED:DEPENDENCY_UNAVAILABLE` (celek D); instance purgnuta po ověření. Poznámka do W18: KV je eventuálně konzistentní, dvojník proto drží čerstvý zápis v paměti isolátu navíc ke KV (read-your-writes jen uvnitř jednoho isolátu, ne napříč nimi) | vady 0; nadvýroba 0; čekání 0 |
| W19 oprava: `ExecutorHost` dedup klíč scoped na capability (Posudek 5, vlastník „up to you") | 2026-09-06 | ≈ 15 min | nezměřeno | ≈ +15/−7 v `src/platform/executor-host.ts` (`dedupKey()`, 4 volání upravena), +2/−2 signatura `remembered(capability, key)` v `tests/idm.test.ts` a `tests/mail.test.ts`, +15 nový test `IDM-HOST-SCOPE-001` | kontrakt 0 / descriptor 0 / testy 15 / boilerplate ≈ 15 / business 0 | 1 (Posudek 5, W19: `document.stamp` a `document.archive` sdílející host mohly kolidovat na stejném `idempotencyKey`; nový test padá bez opravy, ověřeno `git stash` na opravu — `archiveRef` chybí, druhá capabilita se vůbec nespustila) | vady 0; nadvýroba 0 |
| M4b krok 2, celek D1: klienti k DMS a archivu přes HTTP, kryptografická hranice ověřená se skutečným párem klíčů (vlastník „up to you" na rozdělení D) | 2026-09-06 | ≈ 50 min | nezměřeno | ≈ +435: `src/adapters/dms.ts` +60 (`HttpDmsAdapter`), `src/adapters/archive.ts` +30 (`HttpArchiveAdapter`), `src/adapters/fakes-http.ts` ±5 (oprava W22), nový `tests/harness/fakes-world.ts` 45 (sdílený `world()` z celku C, teď dvakrát použitý), `tests/fakes.test.ts` −34/+5 (přesun na sdílený harness), nový `tests/dh.test.ts` 234 (11 testů, Test ID řezu `DH-DMS-*`, `DH-ARCHIVE-*`, `DH-SIGN-*`) | kontrakt 0 / descriptor 0 / testy 234 / boilerplate ≈ 100 / business ≈ 90 (dva adaptéry) | 1 nález (**W22**: `apf-fakes` vyžadoval bearer i na `GET /dms/status` a `/dms/read`, ale `FakeDmsAdapter` (M1) tuhle kontrolu na čtecích metodách nikdy neměl — nekonzistence mezi dvojníkem a jeho HTTP protokolem z celku C, chycena až psaním skutečného klienta; opraveno sladěním protokolu s dvojníkem) | vady 0; nadvýroba 0 |

Poměr po M4: na ~480 řádků business logiky (pět handlerů) připadá 3 084 řádků testů a fixtures a 2 350 řádků platformy. Platforma rostla v M4 jen o 45 řádků; druhá komponenta z jiné domény tedy stála descriptor + handler + fixtures + MUST sadu a **žádnou novou infrastrukturu**, což je přesně cíl VC §7. Cena testů na capability klesla: M3 = 1 184 řádků testů pro 3 capability, M4 = 774 pro 2 capability, protože runner, harness a golden porovnání už existovaly.

Limit 40 h na MUST sadu (VC §7): MUST sada `WRITE_EXECUTOR` je zelená pro tři write capability (`document.stamp`, `mail.ingest`, `email.send`). **Vyhodnocení (6. 9. 2026):** čas vlastníka 4,25 h za celé M0–M4b + AI wall-clock ≈ 3 h 45 min (M1–M4 1 h 50 min, M4b krok 1 1 h 10 min, krok 2 celek 1 45 min) = **≈ 8 h lidského a strojového času dohromady, limit 40 h splněn s velkou rezervou**. Odhad normy (≈ 20 h na MUST sadu, ≈ 30 h na M4) předpokládal solo člověka bez asistence; s asistentem se lidský čas přesouvá z psaní kódu do rozhodování a review (3,5 h ze 4,25 h) a ladění téměř mizí (0,25 h). Mez: odhad vlastníka je zpětný, na čtvrthodiny, a nezahrnuje čas na čtení tohoto souboru.

## Nálezy zachycené testem

| # | Test ID | Co zachytil | Kde by se jinak projevilo | Milník |
|---|---|---|---|---|
| N1 | `EVD-004` | `Audit.all()` vracel mělké kopie; `details` záznamu šlo zvenku přepsat, audit tedy nebyl append-only | provoz: tichá změna evidence (F7), odhalitelné jen forenzně | M3 |
| N2 | `CTR-ERR-001` | descriptor `document.stamp` deklaroval `UNKNOWN_EXTERNAL_OUTCOME`, který executor nikdy nevydává (unknown outcome je status, ne error) | review consumera: kontrakt sliboval kód, na který by nikdo nemohl reagovat | M2 |
| N3 | smoke `INT-E2E-001` (první běh) | prompt jmenoval oddělovače `<untrusted>` ve větě před samotným blokem; adapter (a stejně by mohl model) vzal první výskyt jako data a klasifikoval slovo „and" | provoz: systematická misklasifikace, AI-EVAL by to našel až nad golden setem | M1 |
| N4 | `IDM-DEADLINE-002` | práh skew logu byl `> 5 s`, norma říká „5 s: přijato + skew log" | nikdy (kosmetika), ale test odhalil rozpor s textem normy při prvním čtení | M3 |
| N5 | `IDM-REPLAY-001` (2. případ, napsán po nálezu) | orchestrátor měnil `idempotencyKey` při technickém retry (klíč obsahoval číslo pokusu), v rozporu s §5.2 | provoz: druhý side effect po retry u executora, který dedup opírá o klíč; nalezeno při čtení kódu před testem, test od té doby hlídá | M1 |
| N6 | `SEC-CTX-003` (4. varianta) | router přijímal binding `in-process` bez podpisu, takže podvržená obálka bez signatury prošla | pentest/provoz: obejití celého F4; nalezeno při psaní testu, opraveno `acceptedMechanisms` | M3 |
| N7 | `WF-UNK-001` + `RES-CRASH-001` (návrh) | dedup záznam `UNKNOWN_OUTCOME` v hostu by po reconciliaci „side effect neproběhl" blokoval opětovné vydání se stejným klíčem | provoz: krok by navěky vracel UNKNOWN_OUTCOME; norma o tom mlčí (viz W2) | M1 |
| N8 | `EVD-003` (návrh) | audit `review-created` neměl `correlationId`, celý tok nebyl dohledatelný jedním dotazem | review/incident: operátor by skládal tok ze dvou klíčů | M3 |
| N9 | `INT-E2E-001` scénář `injection-in-allowlist` | injection, která žádá hodnotu **uvnitř** allowlistu, projde hranicí F2 na úrovni classify a bez dalšího signálu se dokument orazítkuje | provoz: mez normy, ne chyba implementace (viz W4); od M4 zachyceno druhým signálem (N11) | M2 |
| N10 | dvě workflow v jednom journalu (`tests/mail.test.ts`, návrh) | `recover()` procházel všechny RUNNING instance bez ohledu na definici; orchestrátor document-intake by po restartu „obnovil" instanci mail-intake se špatnými kroky | provoz po prvním restartu s druhým tokem: krok spuštěný s cizí definicí; nalezeno při zapojení druhého orchestrátoru, opraveno guardem na `workflow` v `run` i `recover` | M4 |
| N11 | `INT-E2E-001` scénáře `injection-in-allowlist` (doc) a `disputed-classification` (mail), `CTR-001` `injection-disputed` | druhý deterministický signál ve validátoru (pravidla nad textem bez instrukčních řádků) chytil oklamaný model v obou tocích a poslal krok do review místo razítka | provoz: bez něj by injection uvnitř allowlistu vedla k zápisu; heuristika, ne důkaz | M4 |
| N12 | `INT-E2E-001` `injection-mail` + `SEC-CTX-003` (první běh M4) | (a) scénář injekce používal fixture s instrukcemi jen v hlavičkách, model neměl co poslechnout a golden lhal; (b) test podvrhoval scope `email.send`, který se v M4 stal legitimním, schéma odmítlo duplicitu dřív než podpis | nikdy v provozu (chyby testů), ale ukazují, že golden a fixtures stárnou s každou další capability a musí se číst jako kód | M4 |

Kontrolní otázka XVI.4 po M4: test zachránil chybu dřív než provoz 5× (N1, N2, N3, N4, N11), 5× zachránilo psaní testu proti normě nebo zapojení druhé komponenty (N5–N8, N10), 2× test odhalil vlastní stárnutí (N12). Jedna mez normy byla doložena (N9) a v řezu překlenuta (N11).

## Co bylo nutné obejít nebo co chybělo

| # | Co | Norma říká | Realita | Návrh pro normu (jen s evidencí odsud) |
|---|---|---|---|---|
| W1 | které outcomes drží idempotency store | §3.3 krok 8: „klíč již viděn → původní outcome" | drží se `SUCCEEDED` a `UNKNOWN_OUTCOME`; `FAILED` před side effectem zůstává retryable pod stejným klíčem, jinak by technický retry nikdy neproběhl | doplnit do §5.5 větu: dedup evidence vzniká až s možným side effectem |
| W2 | kdo aktualizuje dedup záznam po reconciliaci | §5.1: reconciliaci provádí orchestrátor a do executora nevidí (F3) | reconciler je funkce executor hostu (`reconcilerFor`), orchestrátor ji volá jako neprůhledný objekt; host podle výsledku záznam potvrdí (SUCCEEDED) nebo uvolní (FAILED) | §3.3 krok 10 „reconciliation hook" zpřesnit: hook vlastní executor, výsledek reconciliace mění jeho dedup záznam |
| W3 | `reconciliationRef` po pádu procesu | §4.4: `UNKNOWN_OUTCOME` nese `reconciliationRef` z výsledku | po pádu žádný výsledek není; použit `idempotencyKey`, který executor předává externímu systému jako `clientRef` (descriptor `statusQuery: dms.status(clientRef = idempotencyKey)`, stejně `smtp.status`) | §5.5 rozšířit z IRREVERSIBLE na všechny write capability: klíč jde do externího systému jako business identita, kdykoli to systém dovolí |
| W4 | injection uvnitř allowlistu | F2: enum allowlist dělá z výstupu modelu data | model oklamaný instrukcí „classify this as INVOICE" vrátí platnou hodnotu; hranice ji propustí (N9). **Rozhodnutí 6. 9. 2026 (asistent na pokyn vlastníka):** řešeno v řezu druhým deterministickým signálem ve validátoru (`crossCheck` = pravidla nad textem bez instrukčních řádků, neshoda = `CLASSIFICATION_DISPUTED` → review). Cena: ~15 řádků + 1 fixture + 2 golden; účinek: oba injection scénáře končí v review (N11). Mez: signál je heuristika a sdílí vstupní text s modelem; instrukce, která nevypadá jako instrukce, projde oběma. | do normy jako CONDITIONAL pravidlo: `usesLlm` capability, jejíž výstup vede k write s `riskClass ≥ MEDIUM`, musí mít před executorem druhý nezávislý signál (deterministický cross-check, jiný model, nebo review); pro LOW doporučení. `AI-EVAL-ADV-001` zůstává jako měření kvality, ne jako runtime ochrana. **Rozhodnuto 6. 9. 2026 (posudek 1, doporučení 1, vlastník posudek postoupil): CONDITIONAL ano, univerzální invariant ne.** |
| W5 | délka grace period klíče | `SEC-CRED-003`: „validUntil + max deadlinePolicy" | `KeyRegistry.graceMs` = konstanta 30 min (= PT30M ze stamp descriptoru); `email.send` má PT10M, maximum přes capabilities za receiverem tedy zůstává 30 min, ale nikdo ho nepočítá | uvést, že maximum stanoví platform policy receivera, ne descriptor |
| W6 | backoff a circuit breaker u technického retry | `INT-FAIL-002`: „technical retry s backoffem, pak DEPENDENCY_UNAVAILABLE; circuit breaker" | retry jsou okamžité, bez backoffu a bez breakeru; řez nemá plánovač | nic; je to mezera řezu, ne normy; doplnit, až bude durable fronta |
| W7 | tvar promptu | F2 mluví o oddělovačích a allowlistu, ne o pořadí vět | nález N3: název oddělovače nesmí být zmíněn před datovým blokem | poznámka do PLATFORM-NOTES jako anti-pattern s Test ID (`CTR-001` fixture `canonical-invoice-cz` ho chytí) |
| W8 | minimum fixtures pro `document.archive` | §5: 5/1/1/1 pro první capability nové komponenty | 1 canonical, 1 damaged, 2 error; archive není krok workflow, existuje jen kvůli realitě sdíleného hostu | nic; vědomé rozhodnutí proti zásobě, plné minimum až s consumerem |
| W9 | `EVD-006`, `AI-EVAL-*`, `SEC-CRED-001`, `SEC-TOOL-001`, `TEN-*`, `CDC-*`, `WF-VER-002..004`, `WF-COMP-001`, `RES-STOR-002`, `RES-QUEUE-001`, `COMP-DOWN-001`, `SEC-SEM-001` | registr Test ID | neběží: buď nejsou aktivovány profilem (TEN, CDC, COMP, SEM jen pro HIGH), nebo řez nemá mechanismus (migrace, fronta, credential expiry, tooly), nebo potřebují reálný model (AI-EVAL) | nic; CI kontrola `derivedProfiles == executedProfiles` (VC §11) zatím není, seznam výše je ruční |
| W10 | alert při plném úložišti | `RES-STOR-001`: „žádné falešné 202, backpressure + alert" | `STORAGE_FULL` je explicitní FAILED s `retryable: true` (backpressure), ale žádný alert kanál neexistuje; host audituje jen write-done FAILED | doplnit do §7 kategorii záznamu „alert" nebo říct, že alert = security/operational log mimo audit; zatím zapsáno jako mezera řezu |
| W11 | `PRINCIPAL` v jednom procesu | §3.2 rc2.1: PRINCIPAL = vlastní execution context na úrovni credential domény | v řezu = druhý `ExecutorHost` + druhý `CredentialResolver` ve stejném procesu; test `SEC-HOST-001` ukazuje, že mutant resolveru dokumentového hostu na `cred:smtp` nedosáhne, protože ho nedrží; proti cizímu kódu v procesu to ale nechrání (to je PROCESS) | nic pro normu; pro řez: skutečný PRINCIPAL = samostatný deployable (PLATFORM-NOTES §7), pentest podle ADR-017 má běžet až nad ním |
| W12 | kde fyzicky žije to, co je vázané na instalaci | ADR-016: policy je autorita platformy, descriptor claim; o identitách, tenantech, doménách, adresách kanálů a o tom, že se to liší per zákazník, norma mlčí | řez měl granty a allowlist příjemců v `contracts/policy/`, tenanty a identity jako konstanty v `src/slice.ts`, doménu ve `wrangler.jsonc`; nová instalace by znamenala zásah do kódu (pravidlo vlastníka 6. 9. 2026 to zakazuje). **Vyřešeno v řezu 6. 9. 2026 (M4b krok 1):** profil `config/<instalace>/` se schématem a fail-closed sestavením (`src/installation.ts`: policy, granty proti identitám a tenantům, tabulka credentialů = kód říká, co potřebuje, profil, co smí), policy přesunuty do profilu, konstanty ze `src/` pryč, lint v `scripts/arch-dep.mjs` (17 hodnot z obou profilů + e-mail/hostname regex nad `src/**`, farm sources a base `wrangler.jsonc`), overlay `farm.json` per instalace; Worker dostává instalaci build-time aliasem `apf:installation` (modul generuje `farm-config`) |
| W13 | validátor schémat ve Workeru | norma nic (implementační volba) | Ajv kompiluje validátory přes `new Function`; workerd generování kódu zakazuje, `wrangler deploy --dry-run` bundle přesto projde, pád by přišel až při startu Workeru. Nahrazeno `@cfworker/json-schema` (interpret, draft 2020-12; jediný `format` v kontraktech je `date-time`), 207 testů beze změny | do PLATFORM-NOTES: „validátor kontraktů musí být interpret, ne kompilátor kódu, má-li běžet v sandboxu bez eval"; brána portability = běžící Worker (`wrangler dev` + `/version`), ne jen dry-run |
| W14 | `Buffer` v platformovém kódu | norma nic | `@cloudflare/workers-types` deklaruje globální `Buffer: any`; vedle `@types/node` se rozpadnou typy (`toString("hex")` = „Expected 0 arguments"). Platforma přešla na `Uint8Array` + `bytes.ts` (utf8, hex, base64url) a `crypto.getRandomValues`; `node:crypto` zůstává jen pro Ed25519 sign/verify (ověřit ve workerd v celku 3, jinak WebCrypto) | pro PLATFORM-NOTES: přenositelný platformový kód používá WebCrypto/TextEncoder/Uint8Array; `node:*` jen v adaptérech a Node-only souborech (`installation-node.ts`, souborový journal/audit) |
| W15 | synchronní úložiště platformy vs. asynchronní cloud | §5 a §7: journal trvalý před dalším krokem, audit append-only; o synchronnosti norma mlčí | `JournalStore`, `AuditTrail`, `ArtifactWriter` jsou synchronní (orchestrátor volá `journal.put()` bez `await`), R2 a D1 jsou jen asynchronní. Řešení na farmě: orchestrátor běží **uvnitř Durable Objectu instance**, jeho SQLite (synchronní) je journal + audit + artefakty = zdroj pravdy; R2 (originály) a D1 (společný audit) dostávají kopie po běhu (`waitUntil`, idempotentně). Host v jiném Workeru si artefakt přednačte z R2 před voláním synchronního handleru | do PLATFORM-NOTES: „trvalost před dalším krokem" se nejlépe plní úložištěm, které je pro orchestrátor synchronní a lokální (DO SQLite, SQLite soubor); sdílené asynchronní služby jsou kopie, ne zdroj pravdy. Kandidát pro část XVII |
| W16 | extrakce textu z PDF / fotky před classify | norma: vstup capability je artefakt s textem; o tom, kdo a jak z binárního originálu udělá text, mlčí (návrhový list farmy: vstupy e-mail, API, GUI, Telegram foto) | na farmě extrakci dělá gateway při příjmu (`AI.toMarkdown`), výsledek je derivace s provenancí (`derivedFrom`, `producer`), audit `write-intent`/`write-done` pod jménem `document.extract`, ale **bez descriptoru, policy a conformance** (není to capability, jen krok příjmu) | rozhodnout v řezu po krocích B–D: buď `document.extract` jako capability (`usesLlm` pro obrázky = AI_CAPABILITY s F2 hranicí, deterministická pro PDF s textovou vrstvou), nebo ho norma zařadí do „intake" vedle parsování hlaviček mailu; návrh pro normu: extrakce je capability, jakmile obsahuje model (fotky), jinak intake |
| W17 | volba modelu AI je věc instalace, ne kódu; „nikdy bez modelu" | norma: `usesLlm` capability má adaptér modelu a provenance `modelId`; o tom, kdo model vybírá a kde se to konfiguruje, mlčí | profil instalace dostal sekci `models` per capability (provider, model, credential jen jménem, `inferenceGeo`, `processor`), `modelTable()` rozliší dostupné a nedostupné volby (chybějící secret = jmenovaný důvod, nedostupný default = fail-closed), formulář nabízí volbu per dokument, workflow v2 ji nese jako `$input.model`, provenance nese `modelId`; pravidla jsou jen druhý signál a záložní strategie; id modelů se mění pod rukama (Workers AI vyřadila `llama-3.1-8b-instruct` → jen `-fp8`) | do normy: instalační profil obsahuje registr modelů per AI capability s výchozím modelem a zpracovatelem (NIS2 dodavatelský řetězec); `MODEL_UNAVAILABLE` má nést `modelId` a důvod; doporučit periodickou kontrolu existence id modelů (smoke `/version` + testovací dokument) | zavést **instalační profil** jako first-class artefakt normy: identity + scopes, tenanty, policy, kanály, retence, reference na secrets (jen jména); se schématem a fail-closed načítáním; pravidlo „nová instalace = nový profil + secrets, nula změn kódu" ověřitelné lintem (žádná doména, adresa, tenant ani actor id v kódu). Descriptor zůstává v kódu, policy se stěhuje do profilu. |
| W18 | KV jako durable store testovacího dvojníka | norma se dvojníků netýká (D5 není komponenta), ale W15 už řešil totéž pro platformu: „trvalost před dalším krokem“ vyžaduje synchronní zdroj pravdy | KV je eventuálně konzistentní (dokumentace Cloudflare: až desítky sekund mezi lokacemi); `document.stamp` čte stav hned po zápisu (idempotence, `dms.status` po `dms.stamp`), takže `apf-fakes` drží čerstvý zápis navíc v paměti isolátu před KV (`memory` Map v `deploy/cloudflare/apf-fakes/src/index.ts`) — read-your-writes funguje jen v tom isolátu, který zápis provedl, ne napříč nimi na skutečné hraně. Pro dvojníka s jedním voláním na test to stačí; skutečný DMS na farmě (celek D) touto mezerou netrpí, protože se ptá svého vlastního úložiště, ne KV | nic pro normu (D5 je testovací dvojník); do PLATFORM-NOTES jako varování: KV není vhodné úložiště zdroje pravdy pro nic, co čte hned po zápisu; kandidátem by byl Durable Object i pro `apf-fakes`, zatím zbytečné (nižší riziko než u WorkflowInstance) |
| W19 | idempotency store `ExecutorHost` bez scope na capability | norma (§3.3 krok 8, §5.2): „klíč již viděn → původní outcome", ale nemluví o tom, čí je klíč, když jeden host obsluhuje víc capabilit | `ExecutorHost.idempotency` je jedna `Map<string, HandlerOutcome>` klíčovaná jen `idempotencyKey`, sdílená mezi všemi capability registrovanými do téhož hostu (`document.stamp` + `document.archive` v `documentHost`, `src/slice.ts`). Orchestrátor dnes generuje klíč `workflowId:stepId:strategy:logicalAttempt` (`stepId` je jiný pro stamp a archive), takže v běžném toku ke kolizi nedojde — ale je to vlastnost volajícího, ne záruka primitiva. Posudek 5 (externí, nad kódem) to našel a ověřeno přímo v `executor-host.ts:48,148`; žádný test tento scénář nekryje. **Vyřešeno v řezu 6. 9. 2026 (vlastník „up to you"):** `ExecutorHost.dedupKey(capability, idempotencyKey)` skládá interní klíč z obou (`execute()`, `reconcilerFor()`, `remembered()` beze změny na drátě — `message.idempotencyKey` v kontraktu se nemění, jde jen o interní klíč mapy hostu); nový test `IDM-HOST-SCOPE-001` (stejný klíč na `document.stamp` i `document.archive`, obě proběhnou, `archive.putCalls` 1, žádný `duplicate` audit záznam); ověřeno, že bez opravy test padá (dočasný `git stash` na opravu, test selže na `archiveRef` undefined). `IDEMPOTENCY_CONFLICT` při shodném klíči a jiném payloadu zůstává otevřené jako samostatné, oddělitelné rozšíření (fingerprint payloadu by byl nový návrh, ne oprava dnešního nálezu) | do normy: dedup klíč u `ExecutorHost`/WRITE_EXECUTOR musí být odvozený minimálně jako `capability + idempotencyKey`, ne holý `idempotencyKey` (v řezu vyřešeno, kandidát pro část XVII); `IDEMPOTENCY_CONFLICT` při shodném klíči a jiném payloadu jako samostatné, oddělitelné rozšíření |
| W20 | durabilita dedup na Workeru stojí celá na `reconcile()`, ne na lokální mapě hostu | §3.3 krok 10 / §5.1: reconciliation hook existuje pro obnovu po neznámém výsledku, norma nepředepisuje, zda má host navíc durable ledger | `ExecutorHost.idempotency` je `Map` v paměti procesu; na farmě je `apf-document-host` samostatný Worker, kde mezi dvěma požadavky téměř jistě běží jiný isolát — lokální mapa tam prakticky nededupuje nic. Jediná skutečná durabilita je `reconcile()`, který se ptá vnějšího systému (`dms.status(clientRef)`); ověřeno lokálně na fake DMS (`RES-CRASH-001`), ne na skutečném DMS. Posudek 5 (bod 2) upozornil, že bez zapojeného `reconcile()` pro oba handlery na farmě je „exactly-once" tam jen teoretické | podmínka celku D: `reconcile()` musí být zapojený a testovaný pro `document.stamp` i `document.archive` na skutečném Workeru, ne jen ve fakes; pro normu žádná změna, jen ostřejší formulace, že durabilita dedup vychází z `reconcile()`, ne z lokálního stavu executora |
| W21 | vzdálený executor host nemá jak dostat bajty artefaktu ani veřejný podpisový klíč | norma: `ArtifactReader`/`ArtifactWriter` je synchronní rozhraní (platformové úložiště je vždy po ruce); o distribuci veřejného klíče k příjemci, který sám nepodepisuje, mlčí úplně | Rozpoznáno při návrhu celku D, než padl první řádek kódu (proto rozdělen na D1/D2). (a) `document.stamp`/`document.archive` handler čte `deps.artifacts.get(artifactId)` synchronně; na vlastním Workeru `apf-document-host` žádné takové úložiště není, jen `ARTIFACTS: R2Bucket` (async) a `GATEWAY`/`FAKES` service bindingy — bajty artefaktu musí přijít asynchronně dřív, než se spustí synchronní `Router`/`ExecutorHost` řetězec, ne uprostřed něj. (b) instalace bez `apiHost` (`local-fakes`) generuje podpisový klíč nanovo při každém restartu Workeru (`wirePlatform()`), a to per Durable Object instance — vzdálený příjemce nemá stabilní veřejný klíč, ke kterému by se mohl vázat; funguje to jen pro instalace se skutečným, trvalým `GATEWAY_SIGNING_KEY` (`farm-bass443`) | do normy: (a) `unknownOutcomeRecovery`/executor host smí očekávat, že volající (router) mu artefakt už dodá jako součást vstupu, ne že si ho executor sám dotáhne — hranice mezi „platforma dodá data" a „executor je synchronně zpracuje" by měla být explicitní; (b) instalace bez `apiHost` (ephemeral klíč) je ze své podstaty neslučitelná s jakýmkoli vzdáleným přijímačem — norma by měla tuhle podmínku pojmenovat, ne nechat ji objevit až při stavbě prvního vzdáleného hostu |
| W22 | dvojník `apf-fakes` vyžadoval bearer i na čtecí DMS operace, které dvojník v procesu (`FakeDmsAdapter`, M1) nikdy nekontroloval | norma se dvojníků netýká; jde o vnitřní nekonzistenci řezu, ne o normu | `FakeDmsAdapter.status()`/`.read()` nikdy neověřovaly credential (jen `.stamp()` ano), ale `src/adapters/fakes-http.ts` (celek C) vyžadoval bearer na celé `/dms/*` větvi včetně `GET /dms/status` a `/dms/read`. Neprojevilo se to v celku C, protože `tests/fakes.test.ts` vždy posílalo platný token i tam, kde nebyl potřeba; odhaleno až psaním skutečného `HttpDmsAdapter` v celku D1, protože `stamp-handler.ts`'s `reconcile()` volá `dms.status()`/`dms.read()` bez credential parametru vůbec — rozhraní `DmsAdapter.status/read` ho ani nenese | nic pro normu; oprava dvojníka na shodu s dosavadním chováním fake adaptéru (`fakes-http.ts` teď vyžaduje bearer jen na `POST /dms/stamp`) |
| W23 | audit relay `apf-document-host → apf-gateway` (`RelayAudit.append()`) přes `ctx.waitUntil` bez čekání na výsledek na volající straně | §7 audit append-only; `docs/NAVRHOVY-LIST-farma.md` moduly D2–D5: „audit píše každý host přes service binding na gateway `/audit`"; norma o spolehlivosti relay mezi Workery mlčí | Živě na `farm-bass443` (7. 9. 2026, otevřené pozorování ze záznamu (25) ověřeno a potvrzeno jako reálné, ne kosmetické): tři audit eventy (`dispatch`, `write-intent`, `write-done`) pro `document.stamp` se zalogovaly lokálně na `apf-document-host`, ale všechny tři `POST .../audit` dorazily na gateway se stavem `Canceled` (`wrangler tail` puštěný na obou Workerech současně) — ani `.then`/`.catch` v `RelayAudit.append()` se nespustily, isolát byl evidentně recyklován dřív, než `ctx.waitUntil` slib doběhl. Výsledná instance měla `document.stamp` úspěšně proveden (`stampedArtifactId`, `dmsRef` v journalu DO), ale `audit[]` pro tenhle krok byl v D1 trvale prázdný — ověřeno v reálném JSON té instance, ne odvozeno. | do normy: cross-Worker fire-and-forget (`waitUntil` bez explicitního bodu, kde na něj volající počká) není bezpečný vzor pro nic append-only/auditně kritického; norma by měla vyžadovat, že asynchronní relay má vlastní `flush`/bariéru před koncem požadavku, ne jen `waitUntil` samotné |

| W24 | odvozený text (`derive()` v `apf-document-host` i gateway `copyOut()`) se do R2 zapisoval jako `text/plain` bez `charset=utf-8` | §7 evidence: odvozenina je bajty s provenancí, norma o HTTP content-type při čtení mlčí | Vlastník otevřel `/workflow/:id/stamped` u dokumentu s českou diakritikou a dostal mojibake (`Ã¡` místo `á`) — prohlížeč bez explicitního charsetu hádá kódování a na české diakritice typicky uhodne špatně (UTF-8 bajty vykreslené jako Windows-1250). Bajty samotné byly v pořádku (sha256 sedí), jen HTTP hlavička chyběla. Nekryto žádným testem — testy porovnávají řetězce v paměti, ne HTTP hlavičky skutečné odpovědi | do normy nic (implementační detail transportu); pro řez zapsat jako pravidlo: kdykoli se text serviruje přes HTTP s odvozeným/dopočítaným content-type, `charset=utf-8` musí být explicitní, ne spoléhat na výchozí chování prohlížeče |
| W25 | deterministický druhý signál (`classifyByRules`) hledal `dph` jako libovolný podřetězec, ne jako celé slovo | W4/CONDITIONAL: druhý signál má chránit před shodou klasifikátoru s injekcí nebo omylem, ne přidávat vlastní falešně pozitivní shodu | Živě na farmě: JSON objednávka s polem `"celkemBezDph": 29600` prošla jako `INVOICE` s jistotou 0,9 a **rovnou se orazítkovala** — `"celkembezdph"` (název pole, ne obsah) obsahuje podřetězec `dph`, takže pravidlo řeklo INVOICE ze jména pole. LLM řeklo INVOICE taky (položky/množství/ceny vypadají fakturovitě), oba signály se shodly na špatné odpovědi, takže `CLASSIFICATION_DISPUTED` se nikdy nespustilo — druhý signál chrání jen před **neshodou**, ne před shodnou chybou obou stran. Nalezeno vlastníkem klikáním na živém výsledku, ne testem. Opraveno na `\bdph\b` (hranice slova) — řeší tenhle konkrétní kolizní řetězec, neřeší obecný případ, kdy se oba signály zmýlí stejně | do normy: druhý deterministický signál založený na hledání klíčových slov v syrovém textu je zranitelný vůči shodě jména pole/proměnné s klíčovým slovem u strukturovaných formátů (JSON/XML) — doporučení: u strukturovaného vstupu hledat klíčová slova jen v hodnotách polí, ne v celém serializovaném textu včetně klíčů. **Nekryto testem** — otevřený dluh, ne vyřešeno |
| W26 | dávkové zpracování inboxu (`processInbox`) nemělo try/catch kolem jednoho souboru | norma o dávkovém zpracování mimo capability nic neříká (mimo rozsah), ale RES-CRASH-001 duch („jeden pád nesmí vzít s sebou zbytek") platí analogicky | Před opravou: pokud `startIntake()` (nebo cokoli v těle smyčky) vyhodilo neočekávanou výjimku místo řízeného `{ok:false}` výsledku, výjimka vyletěla z `for` smyčky ven — zbylé soubory v dávce (až 9 z 10) se ten běh vůbec nezpracovaly a problémový soubor zůstal v `inbox/` k opakovanému selhání příští běh. Nalezeno vlastníkovým dotazem („pokud nahraji soubory a 1 selže, nemůže blokovat další"), ne pozorovaným pádem naživo — opraveno preventivně | mimo normu (aplikační detail dávkového importu); zapsáno jako obecné poučení pro tenhle řez: jakákoli smyčka přes N nezávislých položek musí izolovat selhání položky uvnitř smyčky, ne spoléhat na to, že volaná funkce nikdy nevyhodí |

**W23 vyřešeno 7. 9. 2026:** `RelayAudit` přesunuta z `index.ts` do vlastního `deploy/cloudflare/apf-document-host/src/relay-audit.ts` (mimo jiné proto, aby šla přímo testovat bez wrangler-only aliasu `apf:installation`). `append()` zůstává synchronní (kontrakt `AuditTrail`, FOUNDATION-core §7, beze změny — nic na drátě se nemění), ale každý relay teď eviduje do `pending[]`; nová metoda `flush(): Promise<void>` je všechny čeká. `/dispatch` handler volá `await audit.flush()` těsně před vrácením odpovědi, v obou větvích (úspěch i `catch`), takže se odpověď nikdy nevrátí, dokud audit skutečně nedorazí (nebo selže viditelně — `.then`/`.catch` teď mají šanci doběhnout). `ctx.waitUntil` zůstává jako záloha. Nový test `DH-AUDIT-RELAY-001` (`tests/dh.test.ts`) dokazuje na fake `Fetcher`/`ExecutionContext`, že `flush()` opravdu nevrátí řízení, dokud jsou relaye rozjeté, a teprve po jejich vyřešení.

## Co je zelené (46 Test ID normy + 16 řezu, 232 testů, 13 souborů)

`CTR-001`, `CTR-ERR-001`, `CTR-TIME-001`; `SEC-PRIV-001..002`, `SEC-INJ-001..002`, `SEC-CTX-002..004`, `SEC-ART-001`, `SEC-CRED-002..003`, `SEC-HOST-001..002`; `MUT-PRIV-001`, `MUT-CTX-001`, `MUT-IDM-001..002`, `MUT-HOST-001`; `IDM-REPLAY-001`, `IDM-DEADLINE-001..002`, `IDM-RET-002`, `IDM-STRAT-001`; `WF-UNK-001..003`, `WF-REV-003..004`, `WF-VER-001`; `RES-CRASH-001`, `RES-DEP-001`, `RES-STOR-001`; `INT-FAIL-001..004`, `INT-E2E-001` (2 workflow, 10 scénářů), `INT-REPLACE-001`; `EVD-001..005`; `ARCH-DEP-001` (od M4b krok 1 včetně lintu instalačních hodnot). Test ID řezu mimo normu: `INST-001..005` (instalační profil, tabulka credentialů a workflow definice fail-closed; každá `config/*/` v repu se sestaví; modely per capability: nedostupná volba jmenovaná, nedostupný default = stop; kandidát pro normu, viz W12, W17), `INT-HTTP-001..008` (`document.validate` přes protokol `apf-fakes`: stejné třídy chyb jako `INT-FAIL` přes skutečnou síť, chaos přepínač bez rebuildu, nedosažitelný dvojník a nesmyslná 200 odpověď, protokol DMS a archivu pro celek D), `IDM-HOST-SCOPE-001` (Posudek 5, W19: stejný `idempotencyKey` na dvou capability sdíleného hostu spustí obě, nekříží se), `DH-DMS-*`, `DH-ARCHIVE-*` (celek D1: `HttpDmsAdapter`/`HttpArchiveAdapter` proti protokolu `apf-fakes`, W22 opraveno), `DH-SIGN-*` (celek D1: gateway podepíše skutečným, čerstvě vygenerovaným párem klíčů, zcela oddělený `Router` s vlastním `KeyRegistry` — jen veřejný klíč — ověří a spustí skutečné handlery `document.stamp`/`document.archive`; pozměněný payload po podpisu, cizí klíč a chybějící scope končí `CONTEXT_BINDING_INVALID`/`CAPABILITY_NOT_ALLOWED`), `DH-AUDIT-RELAY-001` (W23: `RelayAudit.flush()` skutečně čeká na každý rozjetý relay POST na fake `Fetcher`/`ExecutionContext`, dřív než handler smí odpovědět). Mimo testy: brána portability kroku 2 = `wrangler dev` nad generovaným configem + `/version` pro každou instalaci (W13); celek C ověřen navíc `wrangler dev -c apf-gateway -c apf-fakes` (skutečný service binding, ne jen dry-run).

MUST sada `WRITE_EXECUTOR` (VC §7) kompletně pro `document.stamp` (LOGICAL), `mail.ingest` (LOGICAL) a `email.send` (PRINCIPAL), včetně MUST mutantů s negativní větví na obou hostech (`tests/mut.test.ts`, `tests/mail.test.ts`). `SEC-CTX-002` mezi oběma toky zelený (brána M4 podle XII.G). Žádný invariant není `UNVERIFIED` z důvodu skipu; neaktivní Test ID jsou vyjmenovány ve W9.
