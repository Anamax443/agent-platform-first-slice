# document.archive/1: compatibility

| Consumer | Provider | Expectation |
|---|---|---|
| none yet | v1 | conformance suite only |

No consumer, no compatibility declaration. Isolation `LOGICAL` in the shared `document-executor-host`; credential `cred:archive-store` is the only reference this handler may resolve (`SEC-HOST-001`).
